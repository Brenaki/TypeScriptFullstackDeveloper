{
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "node build/script.js",
    "dev": "ts-node-dev src/script.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "express": "^4.18.2",
    "ts-node-dev": "^2.0.0"
  }
}
